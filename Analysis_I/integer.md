# Integers
**Definition**: An *integer* is an expression of the form $a -- b$, where $a$ and $b$ are [[natural_number|natural numbers]].

Two integers $a -- b$ and $c -- d$ are considered *equal* if and only if $a + d = b + c$.