# Rational Numbers
**Definition:** A *rational number* is an expression of the form $a // b$, where $a$ and $b$ are [[integer|integers]] and $b \neq 0$; $a // 0$ is not considered a rational number.

Two rational numbers are considered equal $a // b = c // d$ if and only if $ad = bc$.